@model IEnumerable<ReadPostViewModel>

@{
    ViewData["Title"] = "Home";
    ViewData["ActiveNavigationPage"] = ActiveNavigationPage.Home;
}

@foreach(var post in Model){
    <partial name="_PostCard" model="post" />
}